Command line for server:
g++ -std=c++11 server.cpp -o sv
./sv (port number)

Command line for client:
g++ -std=c++11 client.cpp -o cl
./cl localhost (port number)

To exit press ONLY escape. Program will continue otherwise